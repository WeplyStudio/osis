{
  "entities": {
    "teamMember": {
      "title": "Team Member",
      "description": "A member of the OSIS team.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the team member."
        },
        "role": {
          "type": "string",
          "description": "The role of the team member in the organization."
        },
        "image": {
          "type": "string",
          "description": "URL to the team member's profile picture."
        },
        "quote": {
          "type": "string",
          "description": "A quote or hope from the team member."
        }
      },
      "required": [
        "name",
        "role",
        "image"
      ]
    },
    "siteContent": {
      "title": "Site Content",
      "description": "General content for the website.",
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "URL for an image."
        },
        "hint": {
          "type": "string",
          "description": "A hint for AI image generation."
        }
      }
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/teamMembers/{memberId}": {
      "schema": {
        "$ref": "#/entities/teamMember"
      },
      "description": "Stores information about each OSIS team member."
    },
    "/siteContent/{contentId}": {
      "schema": {
        "$ref": "#/entities/siteContent"
      },
      "description": "Stores miscellaneous site content, like the 'About Us' image."
    }
  }
}
